# Runtime artifacts — jangan commmitted
__pycache__/
*.py[cod]
*.pyo

# Editor/OS
*.swp
.DS_Store

# Secrets (add when needed)
.env

# Local overrides
config/qutebrowser/autoconfig.yml
# Hermes skill runtime state (auto-managed, not dotfile material)
skills/.usage.json
skills/.usage.json.lock
skills/.curator_state
skills/.bundled_manifest
